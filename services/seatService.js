/**
 * @param id
 * @param user
 * @param seatNumber
 * @returns {Promise<void>}
 */
async function bookSeat(id, user, seatNumber) {
    try {
        // perform logic here
    }
    catch (error)
    {
        // log errors
        // rollback
    }
}

module.exports = {
    bookSeat,
};
